name: data_platform
services:
  data_platform_spark_master:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    hostname: data-platform-spark-master
    container_name: data_platform_spark_master
    env_file:
      - ./.env
    ports:
      - "8080:8080"
    volumes:
      - ./conf:/opt/spark/conf
      - ${SPARK_JOBS_DIR}/pyspark:/opt/spark/jobs/pyspark:ro
      - ${SPARK_JOBS_DIR}/java:/opt/spark/jobs/java:ro
    command: >
      /opt/spark/bin/spark-class org.apache.spark.deploy.master.Master
      --host data-platform-spark-master
      --port 7077
      --webui-port 8080
    networks:
      - data_platform_net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 20s

  data_platform_spark_worker_1:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    hostname: data-platform-spark-worker-1
    container_name: data_platform_spark_worker_1
    depends_on:
      data_platform_spark_master:
        condition: service_healthy
    ports:
      - "8081:8081"
    env_file:
      - ./.env
    volumes:
      - ./conf:/opt/spark/conf
      - ${SPARK_JOBS_DIR}/pyspark:/opt/spark/jobs/pyspark:ro
      - ${SPARK_JOBS_DIR}/java:/opt/spark/jobs/java:ro
    command: >
      /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker
      --cores 2
      --memory 4G
      --webui-port 8081
      spark://data-platform-spark-master:7077
    networks:
      - data_platform_net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8081/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 20s

  data_platform_spark_worker_2:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    hostname: data-platform-spark-worker-2
    container_name: data_platform_spark_worker_2
    depends_on:
      data_platform_spark_master:
        condition: service_healthy
    ports:
      - "8082:8082"
    env_file:
      - ./.env
    volumes:
      - ./conf:/opt/spark/conf
      - ${SPARK_JOBS_DIR}/pyspark:/opt/spark/jobs/pyspark:ro
      - ${SPARK_JOBS_DIR}/java:/opt/spark/jobs/java:ro
    command: >
      /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker
      --cores 2
      --memory 4G
      --webui-port 8082
      spark://data-platform-spark-master:7077
    networks:
      - data_platform_net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8082/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 20s

  data_platform_spark_client:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    hostname: data-platform-spark-client
    container_name: data_platform_spark_client
    depends_on:
      data_platform_spark_master:
        condition: service_healthy
      data_platform_spark_worker_1:
        condition: service_healthy
      data_platform_spark_worker_2:
        condition: service_healthy
    env_file:
      - ./.env
    volumes:
      - ./conf:/opt/spark/conf
      - ${SPARK_JOBS_DIR}/pyspark:/opt/spark/jobs/pyspark:ro
      - ${SPARK_JOBS_DIR}/java:/opt/spark/jobs/java:ro
    command: ["bash", "-lc", "tail -f /dev/null"]
    networks:
      - data_platform_net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "test -x /opt/spark/bin/spark-submit"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

networks:
  data_platform_net:
    external: true