services:
  trino:
    image: 'trinodb/trino:479'
    container_name: trino
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - '8085:8080'
    volumes:
      - ./etc:/etc/trino
    networks:
      - datagate-net

networks:
  datagate-net:
    external: true