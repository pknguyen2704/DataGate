name: data_platform

services:
  trino:
    image: trinodb/trino:latest
    container_name: data_platform_trino
    ports:
      - "8085:8080"
    volumes:
      - ./conf/catalog:/etc/trino/catalog
    networks:
      - data_platform_net

networks:
  data_platform_net:
    external: true
