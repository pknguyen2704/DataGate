# Timezone
TZ=Asia/Ho_Chi_Minh

# ==============================
# AWS / S3 creds + region (AWS SDK v2)
# ==============================
AWS_ACCESS_KEY_ID=admin
AWS_SECRET_ACCESS_KEY=miniopassword
AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1

# ==============================
# Iceberg Warehouse (S3FileIO requires s3://)
# ==============================
CATALOG_WAREHOUSE=s3://lakehouse/

# Force Iceberg to use AWS SDK v2 S3FileIO
CATALOG_IO__IMPL=org.apache.iceberg.aws.s3.S3FileIO

# ==============================
# S3FileIO settings for MinIO (SDK v2 style)
# ==============================
CATALOG_IO__S3__ENDPOINT=http://minio:9000
CATALOG_IO__S3__PATH_STYLE_ACCESS=true
CATALOG_IO__S3__REGION=us-east-1
CATALOG_IO__S3__SSL__ENABLED=false

# ==============================
# Compatibility keys (many images still read these)
# ==============================
CATALOG_S3_ENDPOINT=http://minio:9000
CATALOG_S3_PATH_STYLE_ACCESS=true

# ==============================
# Catalog Backend (JDBC)
# ==============================
CATALOG_CATALOG__IMPL=org.apache.iceberg.jdbc.JdbcCatalog
CATALOG_URI=jdbc:postgresql://catalog-db:5432/iceberg
CATALOG_JDBC_USER=admin
CATALOG_JDBC_PASSWORD=catalogpassword

# ==============================
# Postgres (only if this env file is also used by postgres container)
# ==============================
POSTGRES_USER=admin
POSTGRES_PASSWORD=catalogpassword
POSTGRES_DB=iceberg