services:
  datasource_postgres:
    image: postgres:18
    container_name: datasource_postgres
    env_file:
      - .env
    ports:
      - "5433:5432"
    volumes:
      - data_source_postgres_data:/var/lib/postgresql
    restart: unless-stopped 
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - datagate-net
volumes:
  data_source_postgres_data:

networks:
  datagate-net:
    external: true