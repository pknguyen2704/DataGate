name: datagate
services:
  datagate_backend:
    build: .
    container_name: datagate_backend
    ports:
      - "8000:8000"
    env_file:
      - ./.env
    depends_on:
      - datagate_database
    volumes:
      - ./app:/app/app
    networks:
      - datagate_net

  datagate_database:
    image: postgres:18
    container_name: datagate_database
    env_file:
      - ./.env
    ports:
      - "5432:5432"
    volumes:
      - database:/var/lib/postgresql
    restart: unless-stopped 
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - datagate_net
volumes:
  database:
networks:
  datagate_net:
    external: true
